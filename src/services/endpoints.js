/* eslint max-lines: ["error", 500] */
import { API_METHODS } from './constants';

const endpoints = {
  
  NewFeeds: {
    method: API_METHODS.GET,
    URI: 'https://hn.algolia.com/api/v1',
    
  },
 
};
export default endpoints;

